server {
    listen 80;
    server_name port-forward-admin;

    root /var/www/html;
    index index.html;

    location /api {
	    proxy_pass http://backend:8888/api;
	}

    location / {
        proxy_pass http://frontend:3000;
    }

}
